#include <vector>

// Takes a input of int vector and prints routenumber to screen.
void print_routes(std::vector<int> routenumber);
// Check for delays with using a routenumber int as input. 
bool check_delay(int routenumber);